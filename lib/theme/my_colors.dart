import 'package:flutter/material.dart';

/// Constantes de couleurs personnalisées pour l'application.
///
/// Note: Cette classe est dépréciée en faveur des couleurs définies
/// dans les fichiers de thème (dark_mode.dart, light_mode.dart).
///
/// Contient des couleurs de base utilisées dans l'interface:
/// - [bleu] - Gris neutre
/// - [vert] - Couleur d'accent turquoise
/// - [rouge] - Couleur d'erreur/alerte
/// - [blanc] - Texte clair
class MyColors {
  static const Color bleu = Color(0xFF737373);
  static const Color vert = Color(0xFF058B8C);
  // static const Color jaune = Color(0xFFFFC107);
  static const Color rouge = Color(0xFFCA4754);
  static const Color blanc = Color(0xFFD1D0C5);
}
